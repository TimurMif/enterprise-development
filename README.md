# Разработка корпоративных приложений — лабораторная работа №1

В рамках первой лабораторной работы реализована объектно-ориентированная модель фитнес-клуба, подготовлены тестовые данные и unit-тесты с использованием LINQ. Данные хранятся в памяти в виде коллекций.

Вариант: **14**

---

## Модель предметной области

Проект моделирует работу фитнес-клуба, включая учет клиентов, тренеров, специализаций, спортивных залов и проведение тренировочных занятий.

### Основные сущности

| Класс | Назначение |
|---|---|
| `Person` | Базовый абстрактный класс для людей |
| `Client` | Клиент фитнес-клуба |
| `Trainer` | Тренер фитнес-клуба |
| `Specialization` | Специализация тренера |
| `GymHall` | Спортивный зал для проведения занятий |
| `TrainingSession` | Запись клиента на тренировочное занятие |

### Перечисления

`Gender` — пол человека:

- Male (мужской)
- Female (женский)

---

## Связи между сущностями

Основные связи предметной области:

```text
Specialization ─── Trainer
                     │
Client ──────── TrainingSession
                     │
GymHall ─────────────┘
```

- `Trainer` → `Specialization`: многие к одному.
- `TrainingSession` → `Client`, `Trainer`, `GymHall`: многие к одному.
- Один клиент, тренер или зал могут встречаться во множестве занятий.
---
## Данные и DataSeeder
Контекст `FitnessClubContext` содержит пять коллекций:
```csharp
public List<Client> Clients { get; set; } = [];
public List<Trainer> Trainers { get; set; } = [];
public List<Specialization> Specializations { get; set; } = [];
public List<GymHall> GymHalls { get; set; } = [];
public List<TrainingSession> TrainingSessions { get; set; } = [];
```
Начальное заполнение выполняется методом:
```csharp
DataSeeder.Seed()
```
В датасете создаются минимум по 10 экземпляров каждого класса.
- 10 специализаций
- 10 спортивных залов
- 15 клиентов
- 10 тренеров
- 30 записей на тренировочные занятия

---
## Тесты

Тесты находятся в проекте `FitnessClub.Tests`. Для повторного использования одного набора данных используется `FitnessClubFixture`. При создании fixture вызывается `DataSeeder.Seed()`, после чего полученный `FitnessClubContext` используется тестами.
### DomainTests
Тесты проверяют корректность созданного датасета, генерацию GUID и основных навигационных связей.
### QueriesTests
В тестах реализованы LINQ-запросы к доменной модели, фильтрация, группировка и сортировка данных.
### Запуск тестов
```csharp
dotnet test
```
### Результат тестов
<img width="923" height="279" alt="Снимок экрана 2026-09-17 в 00 34 19" src="https://github.com/user-attachments/assets/8f4f2976-4b60-41be-9950-e0a1941875d5" />
